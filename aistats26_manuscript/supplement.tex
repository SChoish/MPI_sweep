\documentclass[twoside]{article}
\usepackage{aistats2026}
\usepackage[round]{natbib}
\usepackage[hyphens]{url}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm,mathtools,bm}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{tikz}
\usepackage{xr}
\usetikzlibrary{arrows.meta,positioning,decorations.pathreplacing}
\special{papersize = 8.5in, 11in}
\setlength{\pdfpageheight}{11in}
\setlength{\pdfpagewidth}{8.5in}
\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newtheorem{remark}{Remark}
\newcommand{\E}{\mathbb E}
\newcommand{\R}{\mathbb R}
\newcommand{\M}{\mathcal M}
\newcommand{\D}{\mathcal D}
\newcommand{\A}{\mathcal A}
\newcommand{\sg}{\operatorname{sg}}
\DeclareMathOperator*{\argmin}{arg\,min}
\externaldocument{paper}

\begin{document}
\onecolumn
\aistatstitle{Supplementary Material for ``BAR: Budgeted Actor Refinement Decouples Bootstrap Exposure from Policy Reach''}
\appendix

\section{PROOFS AND ADDITIONAL ANALYSIS}
\label{app:proofs}

\subsection{Behavior anchor and Proposition~\ref{prop:prox_equiv}}
Let $b(s)=\mathbb E[a\mid s]$. The conditional bias--variance identity
\[
\mathbb E[\|\mu(s)-a\|_2^2\mid s]
=\|\mu(s)-b(s)\|_2^2+
\mathbb E[\|a-b(s)\|_2^2\mid s]
\]
proves Eq.~\eqref{eq:bc_decomposition} after averaging over states and dividing
by $d$; its second term is independent of $\mu$. For deterministic policies,
$W_2^2(\delta_{\mu(s)},\delta_{\nu(s)})=\|\mu(s)-\nu(s)\|_2^2$, so
$\mathsf d_d^2$ is a fixed rescaling of integrated squared Wasserstein
distance. Dividing the normalized anchored objective
$-\alpha\mathbb E_s\bar Q(s,\mu(s))+\mathsf d_d^2(\mu,\nu)$ by $\alpha$
gives Eq.~\eqref{eq:prox_operator} with $h=\alpha/2$. More generally,
$-\lambda\mathbb E_s\widehat Q$ gives $h=\lambda C/2$ because
$\widehat Q=C\bar Q$.

\subsection{Inexact estimated-energy margin and critic-error transfer}
By $\varepsilon_k$-optimality relative to the feasible comparator
$\mu_{k-1}$,
\begin{equation}
\begin{split}
-&\frac{2h_k}{C_k}\mathbb E_s\widehat Q(s,\mu_k(s))
+\mathsf d_d^2(\mu_k,\mu_{k-1})\\
&\leq
-\frac{2h_k}{C_k}\mathbb E_s\widehat Q(s,\mu_{k-1}(s))
+\varepsilon_k.
\end{split}
\end{equation}
Rearranging yields Eq.~\eqref{eq:margin}. For $\widehat Q=Q^\dagger+e$,
substitution further gives
\begin{equation}
\begin{split}
m_k^{\widehat Q}
={}&\frac{2h_k}{C_k}\mathbb E_s
 [Q^\dagger(\mu_k)-Q^\dagger(\mu_{k-1})]
 -\mathsf d_d^2(\mu_k,\mu_{k-1})\\
&+\frac{2h_k}{C_k}\mathbb E_s[e(\mu_k)-e(\mu_{k-1})]\\
={}&m_k^{Q^\dagger}
+\frac{2h_k}{C_k}\mathbb E_s[e(\mu_k)-e(\mu_{k-1})].
\end{split}
\end{equation}
This proves Eq.~\eqref{eq:critic_error_margin}. If
$|e(s,a)|\leq\delta_k$ on the realized path, then
\begin{equation}
m_k^{Q^\dagger}\geq-\varepsilon_k-\frac{4h_k}{C_k}\delta_k.
\end{equation}
The result concerns a trusted critic objective. A true-return statement would
require additional occupancy and value-estimation assumptions.

\subsection{Proof of Proposition~\ref{prop:exposure}}
Let $F_{s'}(a)=\min_jQ_j^-(s',a)$. If every
$Q_j^-(s',\cdot)$ is $L(s')$-Lipschitz, then
\begin{equation}
\begin{split}
|F_{s'}(a)-F_{s'}(b)|
&\leq\max_j|Q_j^-(s',a)-Q_j^-(s',b)|\\
&\leq L(s')\|a-b\|_2.
\end{split}
\end{equation}
Therefore,
\begin{equation}
|y_{\mu_1}-y_{\mu_0}|
\leq\gamma L(s')\|\mu_1(s')-\mu_0(s')\|_2.
\end{equation}
Squaring, averaging over dataset transitions, and taking the square root proves
Eq.~\eqref{eq:exposure_bound}. Later actors do not occur because the TD target
conditions only on $\mu_1$.

\subsection{Local explicit--implicit equivalence}
Let $\beta=dh/C$, $g=\nabla_a\widehat Q(a_0)$, and suppose the action
constraint is inactive. The action-metric-matched linearized update is
$\Delta a_{\rm lin}=\beta g$. A statewise exact proximal solution satisfies
\begin{equation}
\Delta a_{\rm prox}
=\beta\nabla_a\widehat Q(a_0+\Delta a_{\rm prox}).
\end{equation}
If the Hessian of $\widehat Q$ is locally Lipschitz near $a_0$,
\begin{equation}
\nabla_a\widehat Q(a_0+\Delta a)
=g+H\Delta a+O(\|\Delta a\|^2).
\end{equation}
Thus
\begin{equation}
(I-\beta H)\Delta a_{\rm prox}
=\beta g+O(\beta\|\Delta a_{\rm prox}\|^2).
\end{equation}
For $\beta\|H\|<1$ and sufficiently small $\beta$,
\begin{equation}
\Delta a_{\rm prox}=\beta g+\beta^2Hg+O(\beta^3),
\end{equation}
whereas $\Delta a_{\rm lin}=\beta g$. With only $C^2$ regularity, the safe
remainder is $o(\beta^2)$ rather than $O(\beta^3)$. Both ideal updates are
first-order schemes for fixed positive $C$, and changing a fixed $C$ only
rescales time. A positive
policy-dependent global $C$ preserves the raw-critic ascent direction under an
adaptive time parameterization in the infinitesimal limit. This derivation is
not applied to the large-budget regime with active projection, ReLU-region
crossings, critic drift, or inaccurate actor solves.

\subsection{Fixed-budget composition for an ideal consistent map}
\begin{proposition}[Local fixed-budget composition]
\label{prop:local_composition}
Let a smooth one-step update in local coordinates satisfy
$\Psi_h(x)=x+h f(x)+h^2p(x)+O(h^3)$. For fixed $K$ and $T\to0$,
\begin{equation}
\begin{aligned}
\Psi_{T/K}^{K}(x_0)
={}&x_0+Tf_0\\
&+T^2\!\left[\frac{K-1}{2K}J_0f_0+\frac{1}{K}p_0\right]+O(T^3),
\end{aligned}
\label{eq:k_comp}
\end{equation}
where $f_0=f(x_0)$ and $J_0=Df(x_0)$. Coordinate explicit Euler has
$p_0=0$, whereas exact backward Euler has $p_0=J_0f_0$. Hence both are
first-order methods with leading fixed-$K$ flow bias proportional to $1/K$.
\end{proposition}

\paragraph{Proof.}
Write $x_j=x_0+jhf_0+O(h^2)$ for fixed $K$, which follows inductively from the
one-step consistency assumption. Smoothness then gives
\begin{equation}
f(x_j)=f_0+jhJ_0f_0+O(h^2).
\end{equation}
Summing the $K$ increments,
\begin{equation}
\begin{aligned}
x_K-x_0
={}&h\sum_{j=0}^{K-1}f(x_j)
+h^2\sum_{j=0}^{K-1}p(x_j)+O(Kh^3)\\
={}&Khf_0+h^2\frac{K(K-1)}{2}J_0f_0\\
&+Kh^2p_0+O(h^3).
\end{aligned}
\end{equation}
where the final remainder uses fixed $K$. Substituting $h=T/K$ proves
Eq.~\eqref{eq:k_comp}. The exact flow satisfies
$x(T)=x_0+Tf_0+\tfrac12T^2J_0f_0+O(T^3)$. Thus explicit Euler has leading
error $-T^2J_0f_0/(2K)$, while backward Euler has
$+T^2J_0f_0/(2K)$. Both remain first-order methods.

\section{EXPERIMENTAL DETAILS}
\label{app:experiments}

\subsection{Algorithms and schedules}
The implementation parameter is $T=\tau=\alpha/2$, and the normalized
budget is split as $h=T/K$. Thus $K=1$ recovers TD3+BC with coefficient
$\alpha=2T$. BAR-Prox uses Eq.~\eqref{eq:actorloss} with a moving reference;
BAR-Lin constructs Eq.~\eqref{eq:linearized_target} and uses MSE regression.
The $K$ actor networks are independently initialized persistent networks with
separate Adam states. Each iteration first updates the online critic. On every second iteration,
one optimizer step is then performed per actor in chain order on the same
minibatch: actor one updates first; its target actor and the updated critic each
receive a Polyak update of $.005$; later actors then use stop-gradient
references from the updated online chain. Equations~\eqref{eq:qnorm} and the main-text linearized scale define the
actual normalization, with safeguard $10^{-6}$. All methods share data, critic
architecture, and $10^6$ critic updates, while a $K$-hop run performs $K$ actor
optimizer steps and is not compute matched.

The ideal proximal map is not a certificate for this live chain. For a later
persistent actor that differs from its predecessor, even $h=0$ leaves
\begin{equation}
\nabla_{\theta_k}\mathbb E_s\!
\left[\|\mu_k(s)-\sg\mu_{k-1}(s)\|_2^2/d\right]\ne0
\end{equation}
in general. One Adam step therefore need not become a near-identity map as the
nominal coefficient vanishes; the ideal analysis supplies local geometry, while
the sweep evaluates the implemented optimizer trajectory.

We use a 256--256 ReLU actor MLP with tanh output and twin 256--256 ReLU critic MLPs, batch size 256, Adam with
learning rate $3\times10^{-4}$, discount $.99$, policy delay 2, target noise
$.2$ and clip $.5$ times the action bound, and state normalization by dataset
mean and standard deviation plus $10^{-3}$. Every reported sweep score is the
ten-episode D4RL normalized mean at the final $10^6$-update checkpoint.
Archived configurations record 50,000-update intermediate evaluation for
K1/P2/P3, mixed 50,000/final-only evaluation for L2, and final-only evaluation
for L3. Exact historical per-run configurations for proximal seeds 2--3 were not
retained; post-hoc diagnostic manifests record audit settings, not the
training launch.
Intermediate cadence does not enter the headline estimand. The 14-value grid
spans $.05$ to $20$.
Both proximal and linearized procedures use seeds 0--3 in the headline score grids.

The nine exact datasets are the Cartesian product of domains
\texttt{halfcheetah}, \texttt{hopper}, and \texttt{walker2d} with suffixes
\texttt{medium-v2}, \texttt{medium-replay-v2}, and \texttt{expert-v2}.
Timeout rows are omitted from transition construction; terminal flags stop
bootstrapping.

\begin{table*}[t]
\caption{Evidence map. Roles distinguish the complete headline score grids
from post-hoc diagnostics and exploratory controls.}
\centering
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}llll@{}}
\toprule
Evidence & Unit / scope & Seeds & Role\\
\midrule
Proximal and linearized scores & $9$ tasks $\times14$ budgets & 0--3 & Headline grids\\
P3 target/final displacement & 270 checkpoints; 90 matched contrasts & 0--1 & Post-hoc full audit\\
P4 target branch & 180 checkpoints; 90 matched contrasts & 0--3 / 0--1 & Post-hoc audit; no full final actor\\
P4 final branch & 24 frontier cells & 0--1 & Post-hoc targeted panel\\
P3 two-actor control & 90 pairs; 180 final scores & 0--1 & Exploratory mechanism control\\
Route and component controls & 8 selected runs & 0--1 & Post-hoc targeted controls\\
\bottomrule
\end{tabular}
\label{tab:evidence_map}
\end{table*}

\subsection{Diagnostic quantities}
On a fixed current-state validation batch with paired dataset actions, we
compute the empirical action-averaged proxies
\[
\begin{aligned}
\widehat D_{\rm critic}
&=\mathbb E_{(s,a)\sim\mathcal D}\frac{1}{d}\|\mu_1(s)-a\|_2^2,\\
\widehat D_{\rm final}
&=\mathbb E_{(s,a)\sim\mathcal D}\frac{1}{d}\|\mu_K(s)-a\|_2^2.
\end{aligned}
\]
These quantities measure the online actor chain relative to its sample anchor.
They are proxies for, rather than direct measurements of, the next-state
target-policy displacement in Proposition~\ref{prop:exposure}: the practical
TD target uses a Polyak target actor and policy-smoothing noise.
By Eq.~\eqref{eq:bc_decomposition}, the population expectation of each proxy
equals squared displacement from the conditional-mean behavior map plus the
same additive conditional-action variance. Population differences therefore
cancel this common floor, but ratios do not: a ratio of sample-anchored proxies
is not a ratio of mean-policy displacements. Finite validation batches add
sampling error to both interpretations.
We additionally evaluate the deterministic Polyak target actor and 64 draws
of clipped target-smoothing noise on 4096 next-state transitions per
checkpoint. For source index $i$, the audit pairs $s'_i=s_{i+1}$ with dataset
action $a_{i+1}$; it excludes timeout rows and excludes true terminal rows from
this bootstrap-only sample. All target-exposure quantities remain squared
displacement per action coordinate relative to the paired next dataset action. Thus absolute
levels include conditional action variance, whereas paired differences use a
common sampled anchor.
For BAR-Lin, projection attenuation is the RMS norm ratio between the clipped
and raw target displacements. If $\widetilde g$ is the normalized critic action
gradient and $\Delta a$ is the learned actor displacement, define
\[
h_{\rm eff}:=
\frac{\mathbb E\langle\Delta a,\widetilde g\rangle}
{\mathbb E\|\widetilde g\|_2^2}.
\]
We report $h_{\rm eff}/(dT/K)$ as the effective fraction of the nominal
linear-target coefficient. The matched audit evaluates $512$ fixed states per environment--seed run. We also record action saturation, TD-error quantiles, critic magnitude, critic loss, and gains under both the jointly trained critic and a controlled common critic: the frozen final TD3+BC $T=1$ critic from a separate canonical reference run outside the fourteen-value sweep, matched by environment and seed. A run-total gain is the sum of mean $Q$ changes across the $K$ hops on this shared batch. The common critic supports cross-critic comparison but is not treated as ground truth. A run is descriptively labeled collapsed only from its external normalized return, using the threshold $20$.

For the exploratory simulator-reference audit, we clone MuJoCo integration
state and environment RNG from evaluation-policy trajectories, record the
episode clock, and reset the TimeLimit for a continuing-task critic-calibration
estimand. At each state we compare $a_1=\mu_1(s)$ and
$a_K=\mu_K(s)$ under continuations $\rho_1$, $\mu_1$, and $\mu_K$, where
$\rho_1$ is the Polyak first target actor with TD3 clipped target-smoothing
noise. The primary calibration target is the discounted return under this
effective backup continuation. The screening covers three environments,
$T\in\{4,7,10,14,20\}$, $K\in\{2,3\}$, two seeds, and 12 states per
checkpoint. It uses a 1000-step horizon, $\gamma=.99$, four common-random-number
target-smoothing rollouts, and a one-return-unit ranking dead zone. It is a
post-hoc screening study, not a causal routing intervention.

The route-only intervention trains two actor-detached shadow critics from identical initial parameters on the same minibatches and target-smoothing draws while the primary first-hop critic continues to drive the shared actor trajectory. The four environment--budget cells---Hopper-medium at $T\in\{10,20\}$ and Walker2d-medium at $T\in\{4,7\}$, each with seeds 0--1---were selected post hoc from visible stability-frontier transitions, so inference is descriptive of this targeted sample. One shadow uses the first-hop target actor and the other the final-hop target actor. The simulator audit evaluates 12 cloned states at four checkpoints in each of eight runs; inference aggregates checkpoint-level state medians to the training-run level.

For route-native evaluation, $\rho_1$ and $\rho_K$ are respectively the Polyak first and final target actors with the same TD3 clipped target-smoothing distribution. The common-continuation rerun instead evaluates both shadow critics under $\rho_1$ on the same sampled states and rollout seeds.

The compute-matched study uses the same eight final cells. Sequential $K=3$ performs actor updates anchored at $\mu_0,\mu_1,$ and $\mu_2$; the fixed-reference control anchors both later actors at $\mu_1$; the direct-final control applies two optimizer calls to one $2T/3$ objective anchored at $\mu_1$. Every condition performs three actor optimizer calls per delayed update. The target-lag control instead keeps $K=1$ and changes only the actor-target Polyak rate from $.005$ to $.005/3$, leaving the critic-target rate unchanged.

The broader policy-separation control covers all nine tasks,
$T\in\{4,7,10,14,20\}$, and seeds 0--1 at one million updates. Its independently
initialized target and evaluation actors are both anchored directly to dataset
actions. The target actor uses coefficient $T/3$ and alone supplies Bellman
targets; the deployed actor uses $T$. We pair it by task, budget, and seed with
a contemporaneous BAR-P3 rerun under the same evaluation protocol. It performs
two rather than three actor optimizer calls and follows a separately realized
critic trajectory. It is therefore an MCEP-inspired policy-separation
mechanism control, not a published-MCEP reproduction, a compute-matched
re-centering ablation, or an exact shared-trajectory comparison.

The frozen-critic small-step audit uses the final TD3+BC $T=1$ critic for all nine environments and two seeds. At 512 interior dataset actions per run it compares the projected explicit step with a projected fixed-point solution of the implicit action equation for $h\in\{.001,.003,.01,.03,.1\}$. This is an action-level local-consistency test, not a return or coupled-training experiment.

\subsection{Estimands and uncertainty}
The primary high-budget estimand uses the upper seven values of the ordered
fourteen-point grid, $T\ge4$. Within each task it averages the available seeds
and those seven budgets, then weights the nine task means equally. This is an
operational stress-region aggregate, not a claim of pointwise monotonicity in
$T$ or task. A seed-mean environment--budget cell is called collapsed when its
normalized score is below 20. Thresholds 0, 10, 20, 30, and 40 expose
sensitivity to that descriptive boundary.

For score contrasts we resample the nine task-level high-budget means with
replacement for 100,000 draws using analysis seed 20260830. Raw-run collapse
risk is first averaged within each task and then resampled by task using the
same draw count and seed. These 95\% percentile intervals preserve the complete
within-task budget and seed structure but do not remove dependence among the
three datasets sharing one dynamics family. With only three families, family
analyses are sensitivity checks rather than a three-cluster bootstrap. A
secondary task-then-seed bootstrap uses 200,000 draws and seed 20260902: after
averaging the fixed budget set within each seed, it resamples nine tasks and
independently resamples four seeds with replacement for each procedure and each
task occurrence. This targets marginal procedure means rather than treating
seed labels as exact trajectory pairs.
Cross-$K$ means estimate each randomized procedure's marginal performance.
Identical critic initialization
across depth is not required for validity; common random numbers would reduce
variance. Paired rescue counts remain descriptive under the realized seed
coupling.

\section{ADDITIONAL RESULTS}
\label{app:results}

\begin{table*}[t]
\caption{Mean score over nine tasks at each total budget. Every row uses four seeds.}
\centering
\small
\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{r|rrrrrrrrrrrrrr}
\toprule
$T$&.05&.1&.2&.4&.7&1.5&2.5&4&7&10&12&14&17&20\\
\midrule
TD3+BC&63.81&62.57&64.58&65.94&67.69&74.79&67.41&45.93&30.36&23.77&21.64&18.96&18.92&18.09\\
Prox-2&60.45&64.19&67.90&69.61&73.65&78.05&80.66&74.48&58.88&41.14&32.17&29.55&25.95&26.25\\
Prox-3&60.14&60.82&68.08&71.01&73.32&78.95&81.41&81.26&71.00&51.14&49.74&41.68&32.75&35.83\\
Prox-4&59.59&63.95&68.71&71.26&73.26&77.70&81.25&82.06&81.79&74.35&67.88&54.28&52.14&35.29\\
Lin-2&62.28&63.45&66.62&71.63&70.98&75.13&78.43&75.16&51.49&37.81&26.03&27.46&27.50&23.55\\
Lin-3&59.27&62.86&64.99&70.90&74.13&77.74&78.20&78.16&67.80&45.60&39.96&41.69&26.28&25.59\\
\bottomrule
\end{tabular}
\label{tab:tau_means}
\end{table*}

\paragraph{Practical budget summaries.}
At TD3+BC's best shared grid budget, $T=1.5$, P3 and P4 score $78.95$ and
$77.70$ versus $74.79$; the descriptive method-wise grid maxima are $82.06$
for P4 at $T=4$ and $74.79$ for TD3+BC at $T=1.5$. To reduce same-grid
selection optimism, a post-hoc leave-one-dynamics-family-out sensitivity
selects one $T$ per method on the other six tasks and evaluates it on the three
held-out tasks. TD3+BC selects $T=1.5$ in all folds; P4 selects $T=2.5,7,4$
when Hopper, HalfCheetah, and Walker2d are held out. The corresponding P4 minus
TD3 family differences are $+17.08,-8.94,+2.83$, giving $78.44$ versus
$74.79$ overall ($+3.65$; 7/9 task differences positive). This transfer
sensitivity is not a pre-registered selection rule and exposes material family
dependence.

A task-then-independent-seed bootstrap sensitivity preserves the high-budget
P4--P3 and L3--L2 conclusions, with intervals $[3.04,22.73]$ and
$[2.90,14.07]$. At the common $T=1.5$, P4 minus TD3+BC is $+2.92$ with
interval $[-.20,8.46]$, so that fixed-budget practical contrast remains
unresolved at four seeds even though its point estimate is positive.

\begin{table}[t]
\caption{Collapse-count sensitivity for $T\ge4$. Entries are seed-mean cells / raw seeded runs, with denominators 63 / 252 for every method.}
\centering
\small
\begin{tabular}{r|rrrrrr}
\toprule
Threshold & TD3 & Prox-2 & Prox-3 & Prox-4 & Lin-2 & Lin-3\\
\midrule
0  &1/20&0/14&0/13&0/10&1/17&2/23\\
10 &30/134&22/103&15/81&6/50&24/105&17/85\\
20 &34/142&22/111&16/84&8/54&25/112&21/95\\
30 &36/149&26/115&18/88&11/55&29/120&24/99\\
40 &38/165&30/120&19/96&14/58&32/125&24/105\\
\bottomrule
\end{tabular}
\label{tab:collapse_sensitivity}
\end{table}


Beyond the main-text P4--P3 contrast, P4 minus TD3+BC gains $38.59$ points
with task-resampling interval $[21.39,54.63]$ and eight of nine positive task
means. At the score-20 threshold, the P4--P3 and L3--L2 raw collapse-risk
differences are $-.119$ ($[-.218,-.024]$) and $-.067$ ($[-.131,-.004]$).
BAR-Lin is positive in $45/63$ continuous cell contrasts (median $+1.37$);
its five rescues and one reversal account for $42.8\%$ of the total gain.
Thresholded transitions remain descriptive.

\begin{table}[t]
\caption{Per-environment high-budget ($T\ge4$) means.}
\centering
\small
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lrrrrrr}
\toprule
Environment&TD3&Prox-2&Prox-3&Prox-4&Lin-2&Lin-3\\
\midrule
Hopper-M&11.1&28.7&63.7&84.7&28.0&54.0\\
Hopper-MR&45.6&92.3&99.6&99.6&93.0&99.0\\
Hopper-E&3.1&16.8&28.2&46.4&20.8&27.3\\
HalfCheetah-M&58.8&58.9&58.9&58.2&53.2&55.8\\
HalfCheetah-MR&51.0&51.8&51.8&52.6&46.3&48.8\\
HalfCheetah-E&19.9&23.3&30.5&32.8&20.6&27.6\\
Walker2d-M&9.3&26.0&37.2&59.8&9.0&24.9\\
Walker2d-MR&27.3&51.2&72.4&76.8&53.4&52.3\\
Walker2d-E&2.2&21.8&25.1&65.1&21.6&28.2\\
\bottomrule
\end{tabular}
\label{tab:env_high}
\end{table}

\begin{table}[t]
\caption{Core matched $K=2$ diagnostic medians over
$T\in\{4,7,10,14,20\}$. Stable and collapsed groups are defined by external
return, while the remaining rows diagnose the learned trajectory.}
\centering
\small
\setlength{\tabcolsep}{2pt}
\begin{tabular}{lrrrr}
\toprule
&Lin st.&Lin col.&Prox st.&Prox col.\\
\midrule
No. runs&52&38&59&31\\
TD p99&270&$9.6\!\times\!10^{23}$&160.8&$5.0\!\times\!10^{21}$\\
$\widehat D_{\rm critic}$&.112&.538&.064&.446\\
$\widehat D_{\rm final}$&.147&.626&.094&.578\\
Common-$Q$ gain&$+2.11$&$-1.42$&$+2.51$&$-1.06$\\
\bottomrule
\end{tabular}
\label{tab:diagnostic_medians}
\end{table}

A restricted four-environment seed-2/3 archive (80 P2/P3 runs) reproduces the
reference-critic split: all 45 stable gains are positive and all 35 collapsed
gains negative (medians $+1.19$ and $-4.20$). It is a sensitivity check of the
same implementation, not an independent replication.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/exposure_frontier.pdf}
\caption{Realized movement. (a) The proximal realization produces a smaller
current-state first-hop displacement proxy for $T\ge7$. (b) On Hopper-medium,
$K=3$ either increases final reach at approximately fixed displacement or
contracts both displacements in the observed score-transition region. The
displayed current-state quantities exclude Polyak target lag and smoothing
noise. Labels indicate $T$ and marker size is proportional to score.}
\label{fig:exposure}
\end{figure*}

\begin{table}[t]
\caption{Host-separated full-grid replication. High is the $T\ge4$ mean; Cell is the number of seed-pair-mean cells below 20 out of 63.}
\centering
\small
\begin{tabular}{r|rrrr}
\toprule
$K$ & High 0--1 & High 2--3 & Cell 0--1 & Cell 2--3\\
\midrule
1&25.97&24.79&35&35\\
2&42.02&40.38&25&26\\
3&52.78&51.05&17&16\\
4&63.61&64.33&9&10\\
\bottomrule
\end{tabular}
\label{tab:depth_replication}
\end{table}

Both proximal seed pairs reproduce the strict depth ordering in
Table~\ref{tab:depth_replication}. The linearized transition also repeats, with
gains of $7.23$ ($[1.99,12.47]$) on seeds 0--1 and $8.80$
($[4.01,15.21]$) on seeds 2--3. These are seed replications of one
implementation.
\begin{table}[t]
\caption{Targeted final-return controls on the same eight environment--budget--seed cells. The three $K=3$ rows are compute matched to three actor optimizer calls; the $K=1$ rows differ only in actor-target Polyak rate.}
\centering
\small
\begin{tabular}{lrrr}
\toprule
Condition & Mean & Median & Score $<20$\\
\midrule
Sequential $K=3$ &71.4&88.9&2/8\\
Fixed-ref. $K=3$ &70.0&87.9&2/8\\
Direct $K=3$ &59.4&89.6&3/8\\
\midrule
Standard $K=1$ &13.6&0.8&6/8\\
$3\times$ target lag, $K=1$ &12.0&1.5&7/8\\
\bottomrule
\end{tabular}
\label{tab:targeted_controls}
\end{table}

Across the 90 matched task--budget--seed cells, the two-actor control scores
$59.22$ versus $57.67$ for the contemporaneous BAR-P3 rerun; raw collapse
counts are 21/90 and 23/90, and both have 9/45 seed-mean collapsed cells. The
control-minus-BAR task-equal mean is $+1.56$, with a 95\% task-resampling
interval $[-1.52,5.44]$; only 2/9 task means favor the control.
At the run level the control wins 34 cells, BAR wins 52, and four tie. The
positive mean but negative paired median reflects tail redistribution rather
than a broad cellwise shift: the mean differences are $+5.53$ for seed 0 and
$-2.42$ for seed 1, and the two seeds have opposite signs in 30/45
task--budget pairs. Using normalized return below 20 as collapse, the control
rescues five BAR collapses and introduces three at the run level. After
averaging the two seeds first, both procedures collapse in 9/45 cells, and
none of the 45 paired classifications changes.

Eight Walker2d-expert cells resumed from the matching logged
emergency-checkpoint steps on the CPU recovery path, and the two $T=20$ cells
began there from zero. Removing the entire ten-cell task changes the mean
difference to $+1.83$; because this also
removes a task, it is not a hardware-effect estimate. The contemporaneous
BAR-P3 aggregate is only $.14$ above the released P3 aggregate on these cells,
but their cellwise differences have RMSE $24.03$ and range from $-99.50$ to
$+98.63$. We therefore pair the control only with the contemporaneous rerun.
The higher observed control mean and identical seed-mean collapse count show
that sequential re-centering is not a prerequisite for those two aggregate
outcomes on this grid. The bundled comparison establishes neither equivalence
nor which simpler-procedure element is responsible.

The paired route-only study uses the same eight runs and aggregates four
checkpoints per run. For critic route $r\in\{\mathrm{first},\mathrm{final}\}$
and evaluation continuation $\rho$, define the checkpoint state-median
symmetric relative error
\begin{equation}
E_r^{\rho}=\operatorname{median}_s
\frac{|Q_r(s,a_K)-Q_{\rm MC}^{\rho}(s,a_K)|}
{|Q_r(s,a_K)|+|Q_{\rm MC}^{\rho}(s,a_K)|+10^{-12}}.
\end{equation}
The route-native run statistic averages $E_{\rm final}^{\rho_K}-E_{\rm first}^{\rho_1}$ over four checkpoints: final-route error is larger in all four-checkpoint aggregates but only five of eight final checkpoints. The run-level mean is $.124$, median $.013$, exact two-sided sign-flip $p=.0078$, and run-bootstrap mean 95\% interval $[.008,.323]$.
Because this native comparison changes both critic route and continuation, it is not a common-value comparison.
We therefore reran both critics under $\rho_1$. The common-continuation difference is positive in six of eight runs, with mean $.116$, median $.0022$, sign-flip $p=.164$, and bootstrap interval $[.0003,.317]$.
The pre-specified support rule requires a positive mean and two-sided exact sign-flip $p<.05$; the common-continuation result fails the second condition. Neither comparison estimates a return effect because the shadows do not feed back into actor training.

In the frozen-critic audit, 15 of 18 environment--seed runs have an eligible
converged, projection-free point. Every eligible run has maximum local relative
explicit--implicit discrepancy below $2.7\%$, with median run maximum $.55\%$.
The remaining three have no eligible point. This supports local numerical
agreement, not a universal convergence rate.

\subsection{Direct target-action displacement and simulator-reference screening}

The original direct audit covers 270 method checkpoints: TD3+BC, P2, and P3
over nine environments, five budgets, and seeds 0--1. A second dump covers all
180 P4 checkpoints over the same environments and budgets with seeds 0--3.
P4 comparisons to TD3/P3 use the 90 matched seed-0/1 cells.
Table~\ref{tab:direct_exposure} separates critic-target exposure from
final-policy reach. Ratios are descriptive sample-anchored squared-displacement
ratios, not conditional-mean policy distances.

\begin{table}[t]
\caption{Matched-cell audit of deterministic next-state Polyak-target action
and current-state final-policy displacement. The Lower column counts runs in
which the left method has the smaller quantity.}
\centering
\small
\begin{tabular}{@{}llrr@{}}
\toprule
Comparison & Quantity & Lower & Ratio\\
\midrule
P3 / TD3 & Target disp. & 88/90 & .658\\
P3 / TD3 & Final disp. & 36/90 & 1.057\\
P3 / P2 & Target disp. & 84/90 & .903\\
\midrule
P4 / TD3 & Target disp. & 89/90 & .568\\
P4 / P3 & Target disp. & 85/90 & .950\\
\bottomrule
\end{tabular}
\label{tab:direct_exposure}
\end{table}

A restricted four-environment seed-2/3 extension reproduces the P3/P2
target-action-displacement direction in 36/40 cells (median ratio $.905$). It is a post-hoc
sensitivity check, not a new full-grid estimand.

The full P4 dump records the target actor and a current-state first-branch
proxy, not the final actor. On the 24 overlapping frontier cells with saved
final-actor geometry, P4 has lower final displacement in 13/24 comparisons
with TD3+BC (median ratio $.468$) and 5/24 with P3 ($1.062$). Across all 180
P4 cells, target-displacement medians are $.051$ for 144 stable runs and $.130$
for 36 collapsed runs. Thus the 90-cell target contraction does not establish
universal final-policy contraction.

For the final-policy proxy, the median P3/TD3 ratio is $1.057$, whereas the
arithmetic mean difference is $-.104$ with task--seed-cluster interval
$[-.194,-.026]$; a few large contractions dominate the mean.

The current-state proxy and deterministic target exposure have Pearson
correlation $.9999$ over all 270 cells; their median ratio is $.997$ and the
largest cellwise relative difference is $5.7\%$. The corresponding
correlation after target smoothing is $.9993$. The close alignment is an
empirical property of these checkpoints, where Polyak lag is small, rather
than a mathematical equivalence. Among the 84 Prox-3/Prox-2 runs in which
Prox-3 lowers deterministic target exposure, Prox-3 is nevertheless worse by
more than one score point in 17. This rejects the stronger interpretation
that minimizing exposure alone determines performance.

Across 55 stable and five collapsed checkpoints (720 states), the median
checkpoint-level signed critic errors are $-41.4$ and
$+1.70\times10^{12}$; 58/60 episode-cluster intervals exclude zero.
Restoring episode clocks changes 21 checkpoint medians but preserves all 60
signs (overall median $-36.4$). The action-ranking subset is too sparse for a
population claim: only 69/720 states clear the dead zone for both actions, with
29 sign disagreements.
\end{document}
