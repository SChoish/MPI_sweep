[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "mpi-sweep"
version = "1.1.0"
description = "Implicit and matched explicit multi-step policy improvement sweeps in JAX"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{name = "MPI Sweep contributors"}]
dependencies = [
  "flax>=0.10",
  "gymnasium[mujoco]>=1.0",
  "h5py>=3.10",
  "jax>=0.4.30",
  "numpy>=1.26",
  "optax>=0.2.3",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[project.scripts]
mpi-sweep = "launch_mpi_sweep:main"
mpi-train = "train_td3bc:main"
mpi-iql-train = "train_iql_mpi:main"

[tool.setuptools]
py-modules = ["d4rl_data", "launch_mpi_sweep", "tau_grids", "train_td3bc", "provenance", "iql_mpi", "iql_mpi_config", "train_iql_mpi"]
license-files = ["LICENSE"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
