# AntMaze MPI T-sweep (offrl host)

BAR / implicit · GPU0 · 2 slots · 1M steps · final eval only.

## medium-diverse · seeds 0–1 · K=1..4

Partial snapshot while K=4 τ∈{17,20} finishes. Matrices under `K=*/Imp/seed{0,1}.csv`.

Queue after this phase: md seeds 2–3, then large-diverse seeds 0–1 and 2–3.
