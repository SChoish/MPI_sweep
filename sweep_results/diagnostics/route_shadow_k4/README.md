# K=4 route-native simulator audit (compact archive)

Source: `mpi_sweep_lab/audit_report_20260901/route_shadow_mc_k4`.
Estimand: source-native continuation (`rho_1` for `shadow_mu1`, `rho_K` for `shadow_muk`).
Result: 5/8 positive runs, mean delta 0.378, two-sided exact sign-flip p=0.0625.
This misses the pre-specified support rule (mean delta > 0 and p < 0.05).
Do not cite as manuscript evidence. Per-state CSVs are not archived here.
